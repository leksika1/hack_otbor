# LLM layer configuration.
# Without LLM_API_KEY the module falls back to MockLLMProvider and works offline.
LLM_API_KEY=
# Optional: any OpenAI-compatible endpoint (OpenRouter, vLLM, local proxy, ...)
LLM_BASE_URL=
LLM_MODEL=gpt-4o-mini
