# Where the frontend sends API requests.
# Leave it as /api: the dev server and the nginx container both proxy that
# path to the backend, so no host name is baked into the production bundle.
VITE_API_URL=/api
