# --- runtime -------------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9     # multipart upload for POST /analyze
pydantic>=2.6
python-dotenv>=1.0          # loads .env for the LLM_* variables
openai>=1.40                # imported lazily; only needed when LLM_API_KEY is set

# --- tests ---------------------------------------------------------------
pytest>=8.0
httpx>=0.27                 # required by fastapi.testclient
