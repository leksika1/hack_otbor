# Runtime dependencies (what the backend image installs).
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9   # multipart upload for POST /api/analyze
pydantic>=2.6
python-dotenv>=1.0        # reads .env in local development
openai>=1.40              # imported lazily; only used when LLM_API_KEY is set
